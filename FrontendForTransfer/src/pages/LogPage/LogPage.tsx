import LogButton from "../../UI/LogButton/LogButton.tsx";


const LogPage = () => {
    return (
        <div className="flex justify-center h-screen w-screen items-center">
            <LogButton />
        </div>
    );
};

export default LogPage;